<Window x:Class="DiscordVocalOverlay.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:viewmodels="clr-namespace:DiscordVocalOverlay.ViewModels"
        Title="Settings"
        Width="450" Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#2B2D31">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ui:ThemeResources />
                <ui:XamlControlsResources />
            </ResourceDictionary.MergedDictionaries>
            
            <Style x:Key="BaseButton" TargetType="Button">
                <Setter Property="Background" Value="#5865F2"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="#4752C4"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="#2B2D31"
            CornerRadius="8"
            BorderThickness="1"
            BorderBrush="#404249"
            Padding="20">
        
        <StackPanel>
            <TextBlock Text="Settings"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Foreground="#FFFFFF"
                       Margin="0,0,0,20"/>
            
            <!-- Recordings Folder -->
            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Recordings Folder"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#B9BBBE"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="{Binding RecordingsFolder}"
                               FontSize="11"
                               Foreground="#99AAB5"
                               TextWrapping="Wrap"/>
                </StackPanel>
                
                <Button Grid.Column="1"
                        Content="Browse..."
                        Command="{Binding BrowseFolderCommand}"
                        Style="{StaticResource BaseButton}"
                        Margin="12,0,0,0"/>
            </Grid>
            
            <!-- Close Button -->
            <Button Content="Close"
                    Click="OnCloseClick"
                    Style="{StaticResource BaseButton}"
                    HorizontalAlignment="Right"
                    Width="100"/>
        </StackPanel>
    </Border>
</Window>
